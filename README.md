## getByLabelText -> getByLabelText will search for the label that matches the given text, then find the element associated with that label.

## getByPlaceHolderText -> getByPlacehHolderText will search for all the elements with the placeholder attribute and find one that matches the text.